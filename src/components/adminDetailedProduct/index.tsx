// import { useParams } from "react-router-dom"

export const AdminDetailedProduct = () => {
    // const { id } = useParams();

    return (
        <>
            <h1>ESTOU FUNCIONANDO pray</h1>
        </>
    )
}